h1. Fraktguide Crawler Tests

These tests crawl from fraktguide front page on localhost and looks for broken links.

The script also logs in as a regular user and crawls the user-pages.

No external links are followed.

Any pages not returning HTTP 200 OK will be registered as an error.

Settings are defined in morebot.xml.

To run
# Start fraktguide at port 8080 in another window
# Start run.sh
